# Ques_1

Controlled React form for `patientName` and `email`.

## Requirements covered
- Uses `useState` for both fields
- Prevents submit when either field is empty
- Logs the submitted values to the console
- Resets both fields after submit

## Usage
Import `App` into your React entry file and render it inside your existing project, or use this folder as the root of a small React exercise.
